# 1. Reduce Gradle Daemon to 4GB (8GB is often overkill and causes system instability)
org.gradle.jvmargs=-Xmx4g -XX:MaxMetaspaceSize=1g -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8

# 2. 🟢 ADD THIS LINE (This is what the error explicitly asked for)
kotlin.daemon.jvmargs=-Xmx2g

# Standard Android settings
android.useAndroidX=true
android.enableJetifier=true